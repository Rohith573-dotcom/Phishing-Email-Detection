# Project 2 - AI-Driven Phishing Email Detection
# Core pipeline needs only the first three; scikit-learn is used solely for the
# verification section, and streamlit only for the optional deployment app.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
notebook>=7.0
streamlit>=1.30
